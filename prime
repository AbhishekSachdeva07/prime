//using funtion to check whether the given number is prime or not.
#include <stdio.h>
int prime(){
    //taking input in function only.
    int n;
    scanf("%d",&n);
    for (int i=2; i<n; ++i){
        if (n%i!=0){
            continue;
        }
        else{
            return 1;
        }
    }
    return 0;
}

int main() {
    int ans =prime();
    if (ans==1){
        printf("not Prime");
    }
    else if (ans==0){
        printf("Prime");
    }
    return 0;
}
